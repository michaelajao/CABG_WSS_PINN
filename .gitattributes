# Normalise line endings in the repository to LF regardless of platform.
* text=auto eol=lf

# Data and binary artifacts are never line-ending converted.
*.csv -text
*.png binary
*.pdf binary
*.gz binary
*.pt binary
*.pth binary
